version: '3.7'
services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_PASSWORD=Pgadmin632284
    ports:
      - '5431:5432'
    volumes:
      - my-db:/var/lib/postgresql

volumes:
  my-db: